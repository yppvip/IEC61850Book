# IEC 61850 离线知识库

## 项目目标

本项目是可离线打开的 IEC 61850 技术查阅文档。它面向初学者与工程人员，以可追溯的知识层级解释标准、设备模型、SCL 配置、通信服务和工程应用。

## 内容范围

- 首页采用“顶部一级导航 + 左侧知识树 + 面包屑 + 右侧页内目录”的阅读结构；内容较多时可在一级导航下继续分组。
- 一级栏目为：标准文库、技术知识、数据模型、SCL 配置、通信协议、工程应用和术语索引。
- 页面应从上位概念逐步进入下位对象，明确对象的父级、引用关系、适用边界与下一步阅读方向，帮助无背景读者建立知识脉络。
- 现有专题应归入最合适的栏目；保留正确、原创且有来源边界的解释，删除或修正错误、过时和重复内容。
- `outputs/index.html` 是主入口；所有页面、样式和数据必须可在 `file://` 环境离线使用，不依赖 CDN、在线 API 或服务器。

## 非目标

- 不提供抓包文件导入、报文解析器、实时抓包、报文发送、重放、注入、上传或网络通信功能。
- 协议章节可以作为静态技术知识，解释 MMS、GOOSE、SV/SMV、字段层次和工程引用关系；不得把它包装成可执行的抓包分析工具或现场验收替代品。
- 不根据 IED 名、MAC、APPID、厂商名称或不完整工程片段推断实际业务含义。

## 内容与实现要求

- 中文说明保留必要的 XML 标签、IEC 61850 术语、ASN.1、PDU、GOOSE、SV、MMS 等英文原文。
- 示例必须原创、脱敏，并明确其不等同于可投产工程、设备手册或标准原文。
- 修改入口、文件名或专题归属时，同步检查首页、章节内链接、术语索引和 `outputs/README.md`。
- 使用本地相对路径；保持 UTF-8 编码及窄屏可读性。
- 新页面或重要章节应有清晰的标题、面包屑、上级导航、页内目录和“继续阅读”链接。
